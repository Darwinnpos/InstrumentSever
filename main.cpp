
#include "abstractDevice.h"
#include "stepMotorCfg.h"
#include <iostream>
#include <string>
using namespace std;

int main()
{
    string a;
    std::cout << "Hello World!\n" << endl;
    //

    //vector<DeviceInfo> allDeviceInfo;
    //vector<GroupDeviceInfo> allGroupDeviceInfo;

    //DeviceInfo device1Info, device2Info;
    //device1Info.name = "MotorX";
    //device1Info.device_type = DeviceType::STEP_MOTOR;
    //device1Info.channel = 1;

    //device2Info.name = "PumpX";
    //device2Info.device_type = DeviceType::IO;
    //device2Info.channel = 1;

    //allDeviceInfo.push_back(device1Info);
    //allDeviceInfo.push_back(device2Info);
    //CAbstractDevice::GetInstance()->LoadAbstractDevice(allDeviceInfo,allGroupDeviceInfo);
    //DeviceInfo get_device1Info;
    //
    //get_device1Info = CAbstractDevice::GetInstance()->GetDevcieInfoFromName(device1Info.name);
    //cout << "name = " << get_device1Info.name << endl;
    //if(get_device1Info.device_type == DeviceType::STEP_MOTOR)
    //    cout << "type = " << "Motor" << endl;
    //cout << "channel = " << to_string(get_device1Info.channel) << endl;
    //get_device1Info = CAbstractDevice::GetInstance()->GetDevcieInfoFromName(device2Info.name);
    //cout << "name = " << get_device1Info.name << endl;
    //if (get_device1Info.device_type == DeviceType::IO)
    //    cout << "type = " << "IO" << endl;
    //cout << "channel = " << to_string(get_device1Info.channel) << endl;
}